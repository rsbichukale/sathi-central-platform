server {
    listen 80;
    server_name central.yourdomain.com admin.yourdomain.com sathi.yourdomain.com; # Replace with your actual domain

    # Redirect all HTTP traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name central.yourdomain.com admin.yourdomain.com sathi.yourdomain.com; # Replace with your actual domain

    # SSL Certificates (managed by Let's Encrypt usually)
    # ssl_certificate /etc/letsencrypt/live/central.yourdomain.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/central.yourdomain.com/privkey.pem;
    
    # Modern SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    # Protect the SQLite data directory from direct access
    location ^~ /data/ {
        deny all;
        return 403;
    }

    # Proxy all API requests to the Node.js backend
    location /api/ {
        proxy_pass http://127.0.0.1:9090;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Proxy all other requests to the Node.js backend to serve React static files
    location / {
        proxy_pass http://127.0.0.1:9090;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
